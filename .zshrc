# Loader only. This file is seeded once by link.py and then belongs to the
# machine: installers append their PATH lines below and never touch the repo.
# The configuration itself lives in ~/.zsh/rc.zsh.
source ~/.zsh/rc.zsh
